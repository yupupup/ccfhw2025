## JavaScript 代码特征提取流程

1. JavaScript 文件在 V8 引擎上的运行流程为解析、解释执行、编译执行，其中后两个阶段可能随着优化 - 去优化的发生而不断切换。我们希望从解析代码得到的 AST 中实现特征提取，建立代码特征与最佳编译参数之间的联系。
2. 输入代码经过 Parser 解析得到 AST，V8 在 src/ast 下提供了 ast-traversal-visitor.h 头文件，可以继承并重写其中的 `AstTraversalVisitor` 类来遍历 AST 并收集一些信息。
3. 比较重要的特征可能包括：圈复杂度、最大循环嵌套深度、条件分支密度、平均函数大小、内联候选函数比例、热循环识别、闭包使用密度、动态属性访问比例、数组操作方法分布、类型转换操作密度、DOM 操作密度、事件监听器数量、异步操作模式、API 调用模式、第三方库使用检测等。前 10 个特征大多与 V8 的某些优化策略和编译参数密切相关，后 5 个则是与网页调优这一主题相契合的浏览器端特定特征。
4. TODO: 读懂 V8 AST Node 的全部类型和结构，基于此编写正确的 Visitor 实现特征提取；将原先的 Example - Best Compiling Flags 关系扩展为 Example - Vector of Code Features - Best Compiling Flags。

## 研究编译选项之间的相关性

与具体要执行的代码相独立地，有些编译选项之间呈现正相关 / 负相关等特定关系时，执行性能较好。对这一关系的探究可以在测算 Example - Best Compiling Flags 的同时实现（因为该过程需要在多个不同编译参数的组合下测量数据），通过一些模型从这些数据中提取较为显著的结果。