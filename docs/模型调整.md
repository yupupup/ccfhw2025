- 对embedding向量进行L2归一化，V8配置由于布尔值参数被转换为 `0` 或 `1`，而整数参数则直接使用其原始值，归一化**对于树模型（如本项目中的随机森林）**: **不是必须的**

合并gbemu的两个part文件，进行分数测试-->解决分数none导致输入label为0的问题


- 参数配置归一化？/分别归一化or统一
- qwen embedding层提取：顶层/底层
- embedding向量输出，线性层和直接截取
- 初始样本a：2/10/20
- 训练轮数epoch：50/100/200


a=40/epoch=80  avgloss=0.39
a=2/epoch=200 收敛到0.13左右，测试集效果非常差，avgloss=3.38
（30，100）avg=0.47038127248986383